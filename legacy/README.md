# legacy/

`MedFoco.html` é o aplicativo original, exportado de um Claude Artifact. Ele depende de APIs
internas do Claude (`window.claude.use`) e só funciona dentro do Claude.ai.

- Guardado apenas como **histórico e referência** para a reescrita (Fase 1). **Não editar.**
- A mesma versão está marcada na tag `v0.0.1-artifact`.
- Esta pasta é ignorada pelo lint, pelo formatador e pela verificação de independência de
  plataforma.
